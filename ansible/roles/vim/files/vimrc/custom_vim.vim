set nocompatible
filetype plugin on
syntax on

let g:mapleader = ","

" No left column
set signcolumn=no
set foldcolumn=0

" Set spell warning
set spell

" Prevent automatic closing of [, {, (
let g:AutoPairs = {}

colorscheme slate
